//! Converted from TreeNodesModel XML
//! This file contains only `extern ...` statements.

extern action FindEnemy(in range: float, out pos: Vector3, out found: bool);
extern action AttackAction(in loc: Vector3, ref val: int);
extern action MoveToPos(in target: Vector3, in speed: float);
extern condition HasAmmo(in minAmount: int, in current: int);

